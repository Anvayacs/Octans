# STATISTICS

if query == 'Stats' or 'stats' or 'statistics': # Arithmetic Progression 
    stats = input("What do you need to find out (mean, median, mode, )>> ")

    if stats == mean:
        qtype = input("Is the data grouped")

        if qtype == "no":
